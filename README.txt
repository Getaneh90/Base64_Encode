
Base64 Encode

This program deals with parsing command-line arguments, file input/output, Error handling and String and data manipulation. It encodes lines and wraps every 76 charactes. The data are encoded as described in the standard base64 alphabet in RFC 4648






How to compile program

Run: gcc -std=c99 -o base64enc base64enc.c

